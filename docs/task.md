# Roadmap And History

この文書は repository 全体の roadmap / history index である。
プロダクト仕様の正は [docs/requirements.md](requirements.md)、[docs/spec.md](spec.md)、[docs/specifications/](specifications/) とする。

## Current Product Focus

現在の中心は Local-first な Agent workflow observability である。

- Copilot clients から OTel を収集する。
- collection profile で telemetry routing mode を切り替える。
- `raw-only` を最小必須 profile、`docker-desktop-langfuse` を標準 full profile とする。
- Langfuse で個別 trace を確認する。
- saved raw OTLP JSON から normalized dataset を生成する。
- deterministic candidate pipeline で診断・改善候補を整理する。
- static HTML dashboard を local artifact として生成する。
- Issue #51 の Session foundation を別 subsystem として実装し、SDK/Hook events と
  exact-linked OTel evidence を Session workspace へ統合する。

## Planned Work

| Area | 状態 | 概要 |
| --- | --- | --- |
| User-controlled retention mutation (Issue #90) | **完了（local implementation / validation）** | Canonical contract に従い exact Session/item reads、preview/confirmation、atomic pin/unpin/delete-now、operation/history reads、Local Monitor UI、navigation-only Canvas handoff、Playwright を実装した。validated candidate `4d966472bdbe1ccc27f57c05f3afc268025ed37a` は build 0 warnings/errors、full solution 6,496/6,496（0 failed/skipped）で成功し、独立レビューも PASS。repository-safe evidence と #91/#88 handoff は [Issue #90 closeout](sprints/issue-90-retention-mutation/milestones/M1-integration-security-closeout/review.md) に記録した。push / PR / external Issue closure は未実施。 |
| First-trace Doctor core (Issue #102) | **完了（local main 統合済み）** | [First-Trace Doctor interface](specifications/interfaces/first-trace-doctor.md) と D060 に従い、12 fact family / 20 fixed state を純粋・決定的に評価する共有 `doctor.v1` domain、Config CLI の evaluate + verification 4 command、Local Monitor の対応する 5 route、transactional/restart-safe な SQLite Doctor v1 lifecycle を実装した。focused validation は Doctor 232/232、Config CLI Doctor 159/159、Local Monitor Doctor 66/66、統合前 full solution は 5,334/5,334（0 fail/skip）で成功した。#103/#104 の source-specific candidate producer は compile-shape のみで live 未検証、#105 の proxy/UI は未実装であり、setup、evaluation、verification start の成功だけでは first real trace を保証しない。 |
| Claude Code guided setup (Issue #68) | **完了（local main 統合済み）** | D061 と [Configuration setup interface](specifications/interfaces/configuration-setup.md) に従い、Issue #66 の ownership ledger / transaction に `claude-code` adapter を追加した。interactive CLI と `claude -p` の user settings、明示 opt-in WSL2 loopback routing、mapper 対応済み全 Hook、content gate opt-in、Python/TypeScript Agent SDK guidance を対象とする。HTTP/proxy/UI、first real trace / Doctor（#104）、native macOS/Linux installer は対象外。feature branch の focused/full validation、Release ZIP、security / concurrency / migration / Issue間契約 evidence は [Sprint24 ledger](sprints/sprint24-claude-code-guided-setup/ledger.md) に記録した。 |
| Claude Code first-trace orchestration (Issue #104) | **完了（local main 統合済み）** | [Claude first-trace interface](specifications/interfaces/claude-first-trace.md) に従い、Claude guided setup から source-neutral `first-trace` / `doctor.v1`、実ソース候補観測、exact Session binding、T8 cross-surface/matrix evidence までを frozen candidate `54d758a260f347cc31a3191d342ad509eb62d81f`（branch `claude/issue-104-implementation-447584`）へ実装した。build、Playwright bootstrap、first-run intermittent failure の記録、exact full rerun 5,765/5,765 を含む validation evidence は plan ledger に記録した。local main へは merge `70206ce` で統合済み。実 Claude producer の live execution と #110 の `user_prompt` key 観測は #106 が実施した（下行参照）。 |
| Claude Code live-validation matrix (Issue #106) | **完了（final run 済み・local main 統合済み）** | frozen candidate `54d758a` に対する live 4 ケース matrix を 2026-07-18 に実行した。content-enabled（#107 修正の live 実証、`content_state=available`）、Hook+OTel exact binding（byte-equal `exact_linked` と #109 修正の negative 実証）、restart/reconnect、`--sanitized-only` は passed。gate-disabled は実 producer が `user_prompt` key を `<REDACTED>` placeholder と `user_prompt_length` 付きで送出することを `claude -p` / interactive 両 surface で観測（#110 解決）し、presence-only 導出が `available` を誤導出するため blocked（spec-first follow-up は #110 側の責務）。evidence は [live validation followup record](sprints/issue-106-claude-live-validation-followup.md) の dated section、validation harness は `scripts/validation/issue-106/` に整備した。 |
| Claude Code gate-disabled prompt classification (Issue #110) | **実装・自動検証・authorized live rerun 完了（local main 統合済み、shared main 未統合）** | feature-branch candidate `f500f8d` で、Claude OTel v1 の interaction `user_prompt` が documented exact gate-disabled sentinel のとき、値をコピーせず in-place に `not_captured` と分類する resolver を実装した。focused resolver は 14/14、`SourceCapabilityContractTests` は 19/19、final `SourceCompatibilityIngestionTests` は 17/17、`ClaudeFirstTraceCrossSurfaceTests` は 7/7。基準 full suite の初回には既存・無関係の `MonitorOverview` Playwright failure が 1 件あったが、exact focused rerun は 2/2、exact baseline full rerun は 5,936/5,936。変更後の pinned validation は build 0 warnings/0 errors、Playwright bootstrap exit 0、full solution 5,943/5,943（ConfigCli 4,189 + LocalMonitor 1,491 + Doctor 263、0 failed/skipped）。migration/backfill/public shape/dependency は追加していない。2026-07-19 の1回目の明示承認済み targeted rerun は clean candidate `2c809f0` で producer authentication error（exit `1`）と `scan-leaks.ps1` FAIL により `BLOCKED_EXTERNAL` だった。同日の2回目の authorized rerun は local main `502acd9`、Claude Code `2.1.214`、disposable loopback/temp state で preflight PASS、producer authentication が成功し、gate-enabled `claude -p`（`content_state=available`、回帰確認）、gate-disabled `claude -p`、gate-disabled interactive（非TTY piped stdin による代替駆動、genuine attended TTY は未検証）の3ケースすべてで raw-local の exact `<REDACTED>` sentinel + `user_prompt_length` と sanitized `content_state=not_captured` を確認し、3件の `scan-leaks.ps1` すべて PASS、`cleanup.ps1` PASS（owned process/listener/disposable root 残存なし）。evidence は [live validation followup record](sprints/issue-106-claude-live-validation-followup.md) の `Issue #110 completed live rerun` section。repository no-push rule により origin/shared main は未更新のまま、Issue #110 のクローズも実施していない。#105 は #110 が shared main へ統合されるまで blocked のままであり、現時点で release は主張しない。 |
| Agent Execution Graph (Issue #49) | **完了** | [Trace Agent Execution Graph interface](specifications/interfaces/trace-agent-execution-graph.md) に従い、既存 span projection から deterministic な Agent ownership graph を read-time 導出する sanitized `GET /api/monitor/traces/{traceId}/agent-graph` を追加した。Trace Detail は同 API を唯一の Agent 解釈として、Sub-agent summary、Agent 単位の flow / waterfall、Agent inspector、exact / inferred / unresolved 表示を提供する。graph 障害時は spans-only 表示を維持し、`--sanitized-only` でも hierarchy は利用可能、raw instruction / response は既存 raw-detail 境界の best-effort のみ。独立 read-only review の重要指摘を修正し、再レビューで仕様準拠・コード品質とも承認済み。Issue #53 Canvas Evidence はこの graph を再利用し、独自 ownership 解釈を追加しない。 |
| Canvas Session Evidence (Issue #53) | **実装完了 / 再レビュー修正済み** | [Canvas Session Evidence interface](specifications/interfaces/canvas-session-evidence.md) に従い exact-linked trace、#49 Agent graph、spans 全ページを Evidence tab へ統合した。再レビューの required-present upstream shape validation、Review gate stale race、trace-qualified focus key、API `agent_depth` fact preservation、null-caller unresolved orphan classification を direct RED→GREEN で修正。focused Node 26 tests、Canvas contract 23 tests、full solution 920 tests（301 ConfigCli + 619 LocalMonitor）が成功。Canvas runtime の live UI 確認のみ残る。 |
| Canvas Improvement Proposals (Issue #54) | **完了** | [Canvas Improvement Proposal interface](specifications/interfaces/canvas-improvement-proposals.md) に従い、terminal exact-bound Session evidence に基づく `candidate` / `recommended` / `verified` lifecycle を実装した。proposal は sanitized local-runtime metadata と opaque evidence reference だけを保持し、Recommended は 2 distinct Session evidence と明示 promotion、Verified は #56 専有とする。Canvas helper は token-gated/no-store proxy と inert UI を使い、direct apply / diff / path / snapshot / rollback / git 操作を持たない。独立レビューと再修正後、build、Playwright bootstrap、full solution test 961 passing が成功した。 |
| Canvas Proposal Apply (Issue #55) | **完了** | [Canvas Proposal Apply interface](specifications/interfaces/canvas-proposal-apply.md) と D054 に従い、明示承認済み #54 proposal のみを startup 登録済み user-config / Skill / repository root 内の existing regular file へ適用する。relative-path/reparse/root/base-hash/approval-digest の検証、full diff/hunk selection、snapshot+journal recovery、stale rollback guard、sanitized audit を実装し、Canvas action・git・任意 path・source/diff/path leakage は含めない。破壊系 focused test は 94 件、build → Playwright bootstrap → full solution test は 1,061 件（ConfigCli 301 + LocalMonitor 760）で成功。 |
| Canvas Effect Comparison (Issue #56) | **完了** | [Canvas Effect Comparison interface](specifications/interfaces/canvas-effect-comparison.md) と D055 に従い、exact Session/Run/trace objective receipt、proposal revision、active application receipt、user-confirmed cohort を quality-first で比較する機能を実装した。pre/post 各3件、severe/missing/partial/rollback/stale の fail-closed 判定、quality 同等時の duration/total-token median 10%境界、atomic effect receipt + Verified、case-key drill-downを備える。closeout では focused comparison tests（Node 29、.NET 220）と build 0 warnings/0 errors、Playwright bootstrap、full solution tests 1,226 件（ConfigCli 301 + LocalMonitor 925）が成功した。 |
| Canvas Shell and Review Workspace (Issue #52) | **完了** | [design gate 承認済み（2026-07-11）](sprints/issue-52-canvas-workspace/design-gate/design-gate.md)。[Canvas Session workspace UI interface](specifications/interfaces/canvas-session-workspace-ui.md) に従い session sidebar、4 tab shell、Review、human evaluation と `/analysis` preservation を実装した。Evidence は #53 まで placeholder、Improve/Compare は後続 scope として完了した。 |
| Session Foundation (Issue #51) | **完了** | D051 と [Canvas Session workspace interface](specifications/interfaces/canvas-session-workspace.md) に従い共通 Session / Run / Event モデルへの正規化、additive SQLite Session store、Copilot SDK streaming events adapter、fail-open Hook bridge、ingestion routes / normalization / OTel enrichment / sanitized Session API を実装した。closeout では build 0 errors/0 warnings、Playwright bootstrap exit 0、full solution tests 2,577 passed（ConfigCli 1,198 + LocalMonitor 1,379）が成功。実 surface live validation は [issue-51-session-foundation-live-validation.md](sprints/issue-51-session-foundation-live-validation.md) に未検証項目として記録済み。cross-surface closeout は #57 で扱う。 |
| Source capability contract (Issue #61) | **完了** | JSON Schema 2020-12 と 5 surface の manifest による committed structural/capability contract、canonical semantic contract の authority / provenance / deterministic completeness / safety、cross-reference tests、adapter handoff を確定した。Issue #51 exact identity と Issue #49 ownership を保持し、receiver / adapter / DB / migration / HTTP / proxy / UI DTO は変更しない。仕様・品質・最終統合レビュー済み（live validation は scope 外）。focused SourceCapabilityContractTests 13/13、build 0 warnings/0 errors、Playwright install exit 0、full solution tests 1,250（ConfigCli 314 + LocalMonitor 936）が成功。 |
| Wave 1 repository metadata diagnostics (Issue #58) | **完了（real producer capture は external blocker）** | `6c92070e` で Retention-gated key-only `/diagnostics` inventory、fixed 5-state reason、authoritative `vcs.repository.name`、absent-name-only canonical GitHub HTTPS fallback を実装し、`b85494e5` で repository-safe live evidence を記録した。focused 73/73、full 6,642/6,642。synthetic loopback validation は完了し、real VS Code / authenticated Copilot CLI payload capture は未承認のため `blocked_external`。Evidence: [issue-58-repository-metadata-live-validation.md](sprints/issue-58-repository-metadata-live-validation.md)。 |
| Wave 1 instruction finding handoff (Issue #59) | **完了** | `c87acd85` で closed taxonomy、exact raw-local evidence validation、kind-specific safe reference tokens、supported-only candidate eligibility、fixed-template canonical `instruction-finding-handoff.v1`、atomic SQLite persistence を実装した。Wave 2 compatibility revision `5819a702` は frozen bytes/schema を変えず source-neutral public validator を追加し、#72/#85 が同じ authority を消費する。 |
| Wave 1 historical source policy (Issue #76) | **完了（policy/spec only）** | `865b13a9` と review correction `f7dfe704` で canonical policy、JSON schemas/profiles/fixtures、empty current support sets/allowlists、sparse provenance-exact handoff、fail-closed #77/#78/#79 boundary を確定した。focused 18/18、full 6,600/6,600。Issue #76 自体は adapter を実装せず、positive content parsing/import、migration、background scan、UI は未実装の downstream scope。 |
| Wave 1 alert-rule engine foundation (Issue #80) | **Wave 3 compatibility repair 実装済み（review 待ち）** | `d75742c3` で source-neutral snapshots/capabilities、compiled deterministic registry、bounded suppression、exact-evidence immutable receipts、keyed sensitive labels、component-owned SQLite schema v1 を実装した。Wave 2 compatibility revision `845c9c02` は producer/store bytes と schema v1 を変えず strict public receipt consumer を追加した。Wave 3 candidate は同じ bytes/schema を維持した source-neutral evaluate-and-append application、typed immutable outcome、bounded owner-validated query と fully typed Alert Center receipt projection を追加し、#84 の parser/SQL 複製を不要にする。#81/#82 rule packs、#83 lifecycle、#85 export はこの frozen authority を消費する。 |
| Wave 2 bounded historical extraction (Issue #72) | **完了（local main 統合済み）** | accepted local-only `7197869f`、main merge `aba60c1d`。coherent bounded selection、exact evidence resolution、raw-local/repository-safe canonical pair、checksums、insert-or-identical persistence、#73/#74 handoffを実装した。history import、LLM、UI、proposal/effect/apply は含まない。 |
| Wave 3 historical instruction analysis (Issue #73) | **実装・focused validation 完了（local feature branch、main 未統合）** | persisted #72-only runner、per-Session #59 grounding、credential-safe provenance、distinct terminal states、component v1 persistence、#75 exact read DTO、explicit-provider production composition を実装した。fake provider automated evidence のみを対象とし、genuine provider execution は `blocked_external`。HTTP/UI/proposal/effect/apply と release は主張しない。 |
| Wave 2 GitHub Copilot historical adapter (Issue #77) | **完了（local main 統合済み）** | accepted rebased local-only `1086409a`、main merge `a3a37e5e`。現在の #76 empty support/allowlist に従う explicit metadata-only opt-in detector と deterministic zero-candidate #79 preview を実装した。positive fixture-bound format support は `blocked_external`。 |
| Wave 2 Claude Code historical adapter (Issue #78) | **完了（local main 統合済み）** | accepted rebased local-only `810d9a6b`、main merge `e64e5f08`。#63/#76 semantics に従う explicit metadata-only detector と deterministic zero-candidate #79 preview を実装した。positive fixture-bound transcript support は `blocked_external`。 |
| Wave 3 historical import workflow (Issue #79) | **実装中** | producer preview v1 を不変のまま、別の `historical-import-workflow/v1`、dedicated `historical_import` schema v1、Local Monitor API/UI、Config CLI、preview→confirmation→atomic commit→result/history、separate historical observation read model を spec-first で定義した。current #77/#78 real profiles は zero-candidate / commit-disabled のままで、positive path は将来 exact profile promotion 後の typed trusted admission に限定する。Session/Run/Event/trace/timestamp は合成せず、metadata-only は retention row を作らない。検証・review・local commit は本 issue branch の closeout 時に記録する。 |
| Wave 2 tool/retry alert rules (Issue #81) | **完了（local main 統合済み）** | accepted local-only `f44c72ee`、main merge `e739a8c8`。#80 registry/evaluator/evidence/suppression contract 上に five-rule pack `@1` と境界・悪性 content・partial/capability-gap fixture を追加した。 |
| Wave 2 token/context/cache alert rules (Issue #82) | **完了（local main 統合済み）** | accepted local-only `5e73911b`、main merge `632660ba`。#80 contract 上に five-rule pack `@1` と source-neutral/current-manifest fixture matrix を追加した。real source effective-context-limit mapping は `blocked_external`。 |
| Wave 2 alert lifecycle (Issue #83) | **完了（local main 統合済み）** | accepted rebased local-only `38572014`、main merge `a2d3fdd`。separate `alert_lifecycle` schema v1、append-only events、derived state、CAS/idempotency、explicit reevaluation/supersede/orphan seams、sanitized no-store API を実装した。receipt bytes は不変。 |
| Wave 2 sanitized evidence export (Issue #85) | **完了（local main 統合済み）** | accepted local-only `37a095d7`（functional evidence SHA `56c20332`）、main merge `a7ff741e`。trusted read-only SQLite snapshot、closed #58/#59/#80 carriers、deterministic manifest/archive/checksums、fail-closed scanner、CLI/HTTP、#91 active rowsを実装した。#72/#83 carriers は v1 対象外。 |
| Wave 2 integrated closeout (#72/#77/#78/#81/#82/#83/#85) | **完了** | validation execution SHA `48ac57e6` で skill mirror、build 0 warnings/0 errors、Playwright bootstrap、full solution 7,429/7,429（#59 20、Alerts 418、Doctor 266、ConfigCli 4,374、LocalMonitor 2,351）を確認した。focused は #72 116、#77 55、#78 44、#81 71、#82 172、#83 domain/store 79 + routes 18、#85/#91 86 + CLI 10 が全件成功。同 SHA の初回 full run は setup test barrier の scheduling starvation により 7,426 passed / 3 failed となり、3-test 3/3 と ConfigCli 4,374/4,374 の切り分け後に exact full rerun が成功した。初回 failure は成功結果で置換せず closeout evidence に保持する。 |
| Wave 3 historical efficiency analysis (Issue #74) | **実装完了（full solution 統合検証待ち）** | frozen #72 repository-safe bytes のみを入力にする deterministic 10-category registry（6 supported、4 explicit unavailable）、missing/zero-denominator-not-complete、quality-aware `supported` / `weak` / `incomplete`、driver ごとの extraction ID/hash、exact evidence refs、canonical `historical-efficiency-receipt.v1` と #75 handoff を実装した。実 #72 producer pair の保存・source DB 消失後再読込を含む focused #74/#72 regression は 141/141、hash-bound fixture review、skill mirror、build 0 warnings/0 errors、Playwright bootstrap は成功。full solution suite は統合 coordinator の immutable candidate 検証待ち。AI narrative は `not_applicable`、#91 registry と既存 Issue #75 placeholder は未変更。 |
| Source drift and Claude Code (Issues #62-#65) | **完了（main 統合済み）** | [Sprint22](sprints/sprint22-source-drift-claude/) で ingest-batch fingerprint、bounded unknown diagnostics、Copilot fidelity gate、Claude Code OTel/Hook authority、exact binding、projection/UIを実装した。最終統合は build、Playwright bootstrap、full solution 2,577/2,577 PASS と独立 security / Canvas review PASS を記録している。未検証versionは既知fingerprint一致なら処理し、新fingerprintだけをdegraded化する。Claude live surfaceが利用不能な場合の独立 follow-up と complete trace-context DTO の保留は、完了済み実装の明示的な非ブロッキング follow-up として維持する。 |
| Configuration ownership and GitHub Copilot guided setup (Issues #66-#67) | **完了（local main 統合済み）** | [Configuration setup interface](specifications/interfaces/configuration-setup.md) と D058 に従い、user-scoped versioned ownership ledger、immutable redacted plan、hash-guarded apply/rollback/status、atomic file/current-user environment transaction、および `github-copilot` の VS Code / CLI / caller-managed App-SDK adapter を実装した。Windows x64 Release ZIP は self-contained Config CLI と byte-faithful `setup.ps1` を同梱する。成功 status/storage と real #66→#67 は injected trusted platform による executable in-process production composition で検証し、PATH-stripped package と subprocess の証跡は executable 選択および pre-dispatch transport に限定する（過去の `LOCALAPPDATA` を使う物理 `status_ready` 隔離主張は無効）。Migration A は N/A（schema v1 維持）で、committed v1 fixture の process-equivalent restart と byte identity を確認した。security / concurrency / migration / Issue間契約 / repository-safe evidence の5軸はすべて PASS C0/I0/M0。local main merge-tree の pinned validation は build 7 projects 0 warnings/0 errors、Playwright exit 0、solution tests 4,861/4,861（ConfigCli 3,476 + LocalMonitor 1,385、0 fail/skip）で成功した。HTTP/proxy/UI は N/A、first-trace receipt は主張せず Issue #69 の責務、macOS/Linux native execution は未実施。feature branch は保持し、push / PR / external Issue closure は含まない。 |
| Instruction Diagnosis Conversation Scope | **M5 条件付きPASS（category-shift caveat付きGO）** | [docs/sprints/sprint21-conversation-scope/](sprints/sprint21-conversation-scope/) で、Issue #46 Phase 2 step 2 として `conversation_id` で束ねた sibling traces を `instruction-diagnosis` が bounded に参照できるようにした。Sprint20 の `conversation` metadata は trace id / 順序 / 件数に留まっていたため、Sprint21 では選択 trace を anchor にしつつ、同一 conversation の前後最大 2 trace ずつ（最大 5 entries）を raw analysis 内部の `conversation_context` structured evidence として扱う。新規 public route / `/api/monitor/*` field / projection migration / Canvas focus 変更は行わず、D048 と interface spec に正本化したうえで実装した。M4 regression validation は build 0/0・Playwright bootstrap 成功・full test 728 passing（301 ConfigCli + 427 LocalMonitor）で完了。M5 は preserved Sprint19/Sprint20 DB の A1/A2/B1/B2/C1/C2 を prompt-v4 `instruction-diagnosis` で再実行し、全 6 run が local BYOK provider path で succeeded。39/39 の検出 span refs が分析 trace または emitted bounded sibling window 内で解決し、trace refs もすべて分析 trace または同一 `conversation_id` window 内だった。A2 が 0 findings になり、C2 が sibling C1 evidence により `task-size-split` を出すなど Sprint20 から category distribution は変化したため category-shift caveat として記録するが、citation / grounding / bounded-window / sibling-clarity / data-safety gates は通過。Issue #46 は repository-safe コメント下書きのみ（未投稿）。 |
| Instruction Evidence Extractor | **M5 条件付きPASS（provider caveat付きGO）** | [docs/sprints/sprint20-instruction-evidence-extractor/](sprints/sprint20-instruction-evidence-extractor/) で、Issue #46 Phase 2 step 1 として `instruction-diagnosis` 用の決定的 `get_instruction_evidence` extractor と prompt v3 を追加した。製品 API / schema / route / projection migration は変更せず、process-internal tool と prompt contract の additive 変更に限定。M1-M4 は完了。M5 A/B は preserved Sprint19 DB で再実行し、A1/A2/B1/B2 は BYOK(glm-5.2) で成功、C1/C2 は 900 秒 timeout 後に SDK default(no-BYOK) provider で完走（Local Monitor を .NET process として `bin\Debug\net10.0` から実行し `runtimes\win-x64\native\copilot.exe` を解決。C1=1所見 `missing-context-constraints`・引用4/4実在・extractor grounded、C2=所見0件・no-evidence-no-finding Pass）。C1/C2 の provider/model 差分は provider parity caveat として記録するが、900 秒 timeout は外部 GitHub Copilot/BYOK provider path に局在し、実装 / prompt v3 / extractor grounding の blocker ではないため、M5 は **条件付きPASS / provider caveat付き Phase 2 GO** とする（[evidence](sprints/sprint20-instruction-evidence-extractor/milestones/M5-ab-validation/ab-validation.md)）。Issue #46 は repository-safe コメント下書きのみ（未投稿）。 |
| Instruction Diagnosis Analysis | **完了（Phase 2 判定: GO）** | [docs/sprints/sprint19-instruction-diagnosis-analysis/](sprints/sprint19-instruction-diagnosis-analysis/) で、[Issue #46](https://github.com/fukuda-yuki/copilot-agent-observability/issues/46) Phase 1 として、既存 Local Monitor Copilot raw analysis に `instruction-diagnosis` focus（drawer 表示名: 指示診断）を追加した（D046）。証拠は trace 内部のみ、finding 単位の固定4点形式（分類 / span・turn 証拠引用 / ギャップ説明 / 次回向け改善指示文）、証拠を引用できない finding は出力禁止、taxonomy v1 は 5 分類。additive な focus enum 拡張のみで新規 route / schema / API field なし、Canvas focus set（D036）と `--sanitized-only` の raw analysis 全無効は不変。M5 BYOK live validation（Copilot CLI 実トレース 6 本、succeeded 6 run、所見 9 件、幻覚引用 0）で gate 3 基準（引用実在性 / trace 固有性 / 証拠必須）を通過し、Phase 2 GO を Issue #46 に記録（[evidence](sprints/sprint19-instruction-diagnosis-analysis/milestones/M5-live-validation/live-validation.md)）。M5 中の統合修正として additive `CopilotAnalysis:TimeoutSeconds`（既定 60、spec 反映済み）とプロンプト日本語出力規則の強化を実施。Phase 2 設計インプット: カテゴリ⇔証拠結合の強化（決定的事前抽出）と conversation-id スコープ解析。 |
| Local Monitor Mock Data & User Guide | **完了** | Sprint18 再設計後の Local Monitor を対象に、(1) 合成モックデータ一式（`scripts/demo/payloads/` 9 トレース: リッチトレース ok / recovered / unrecovered、一覧バリエーション 4 件、最小回復ケース、概要用単発）と投入スクリプト `scripts/demo/seed-monitor-mock-data.ps1`（loopback 検証・readiness 確認・`POST /v1/traces` 投入のみ、新規依存なし）を追加し、(2) 7 画面 + sanitized-only 姿勢の live 手動検証を実施（Copilot ドロワーは BYOK 経由で解析成功 + 履歴再送追い質問まで確認）、(3) `docs/user-guide/local-monitor.md` を Console 型 IA（D042–D045）に全面改訂し、README の Local Monitor セクションと `docs/assets/screenshots/` のスクリーンショット（旧 3 枚を新 9 枚に刷新）、getting-started の旧 `/ingestions` 参照を更新した。製品挙動・public interface の変更なし（dev tooling + docs のみ）。既知の特性: 同一 DB への再シードはスパン重複（スクリプト・ガイドに明記）、投入直後は期間トグルが全期間同値。 |
| Local Monitor UI Redesign | **完了（実ペイロード整形表示のライブ検証のみ人手ゲート）** | [docs/sprints/sprint18-local-monitor-ui-redesign/](sprints/sprint18-local-monitor-ui-redesign/) で、Local Ingestion Monitor UI を `.claude/design_handoff_local_monitor/` のデザインハンドオフに従い Console 型 IA（208px サイドバー + 2 項目ナビ + master-detail）へ全面再設計した（D042）。7 画面（概要 / トレース一覧 / トレース詳細フロー・waterfall / スパンインスペクタ / エラー解析モード / Copilot ドロワー / 診断）を実装し、トレース詳細のタブは廃止。バックエンドは monitor projection schema v4（cache token rollup + `trace_status`、additive、backfill なし。D044）、sanitized `GET /api/monitor/overview` / `GET /api/monitor/trace-list`、raw-bearing `GET /traces/{traceId}/spans/{spanId}/detail`（D043）、analysis start payload の `question` / `history`（履歴再送チャット。D045）を追加した。既存 public routes の shape / ordering と `CanvasExtensionContractTests.cs` は不変（無変更で green）。デザイントークンはハンドオフ §10 hex を正とし `DESIGN.md` を更新済み。検証: build 0 警告 / 0 エラー、full `dotnet test CopilotAgentObservability.slnx` 687 passing（301 ConfigCli + 386 LocalMonitor、Playwright smoke 含む）。E2E テストケース・検証記録・既知の制約は [evidence](sprints/sprint18-local-monitor-ui-redesign/) に保存。`SpanDetailExtractor` の実 VS Code Copilot Chat ペイロードでの整形表示ライブ検証のみ人手ゲートとして残る（D043）。 |
| Canvas Analysis UX | **実装中** | [docs/sprints/sprint17-canvas-analysis-ux/](sprints/sprint17-canvas-analysis-ux/) で、Canvas helper の既存 `session.send()` + bounded Canvas actions 分析トリガーを維持したまま requested profile / model / reasoning / timeout hint controls と dispatch metadata を追加する。Local Monitor raw analysis runner への置き換えは non-goal。`GET /api/analysis/options` は sanitized configuration metadata のみを返し、Canvas helper は token-gated proxy で取得して UI / prompt / dispatch metadata に使う。model / reasoning / timeout は per-message execution control ではなく requested values として扱い、effective execution metadata は主張しない。 |
| Canvas Cross-Repo Adapter Packaging | **完了** | [docs/sprints/sprint16-canvas-cross-repo-adapter/](sprints/sprint16-canvas-cross-repo-adapter/) で、GitHub Copilot App Canvas adapter を cross-repo 利用向けの copyable extension distribution unit として整理した。`.github/extensions/otel-monitor-canvas/` を唯一の配布 source of truth とし、extension-local `canvas.json` / README / synthetic preview asset を追加した。Local Monitor projection は resource-scoped `vcs.repository.name`（absent の場合だけ canonical GitHub HTTPS URL fallback）/ `workspace.name` / `repo.snapshot` から sanitized `repository_name` / `workspace_label` / `repo_snapshot` を新規 trace に限って投影し、Canvas helper は extension scope、monitor URL/readiness、repo/workspace label、manual filter、`unknown repository` fallback を表示する。既存 projected rows の自動 backfill、dependency/package manifest 追加、mirror folder、current repo auto-match、raw/PII/path/token の Canvas action/log/repository-safe output 送出は行わない。M5 automated validation は [review](sprints/sprint16-canvas-cross-repo-adapter/milestones/M5-validation-and-closeout/review.md) に記録済みで、build 0/0・full test 621 passing（301 ConfigCli + 320 LocalMonitor）。GitHub Copilot による user-scope / project-scope live Canvas runtime validation は [live validation evidence](sprints/sprint16-canvas-cross-repo-adapter/milestones/M5-validation-and-closeout/live-validation.md) に記録済み。Issue #58 の current diagnostic/fallback evidence は [repository metadata live validation](sprints/issue-58-repository-metadata-live-validation.md) を参照。 |
| Canvas Diagnostic Surface | **実装中** | [docs/sprints/sprint15-canvas-diagnostic-surface/](sprints/sprint15-canvas-diagnostic-surface/) で、Sprint11 の Canvas adapter を Local Monitor の既存 API / view model / projection を再利用した診断 surface へ引き上げる親 Epic。「Sprint12 親 Issue」として起票されたが Sprint12 は完了済みのため Sprint15 として新設（D036）。子 A〜E に分割し、本スプリントでは表示境界を変えない子 A（拡張所有ヘルパーページの UX 改善：判断できる trace 一覧、focus / ボタン / 見出しの日本語化、health / error 状態と次操作の具体化、health 生レスポンスの折りたたみ）を実装した。子 A 着手前に技術負債 F8（JS smoke 不足）へ対応済み：`extension.mjs` から純関数を `canvas-helpers.mjs` へ抽出し、`canvas-helpers.test.mjs`（`node --test`）と `CanvasExtensionContractTests.cs` の node 実行テスト（`node --check` x2 + `node --test`）を `dotnet test` ゲートに統合した。D037 で子 B〜E の設計を確定し（GitHub Copilot SDK `rpc.ts` 生成型調査、Local Monitor 既存実装調査、OTel 取り込み側の識別子調査を実施）、子 B・子 C を M2/M3 として並列実装した。子 B: 新規 sanitized endpoint `GET /api/monitor/summary`（`MonitorSummaryService` 共有、Razor `Index` PageModel と共用、per-model / per-client-kind 集計、readiness は含めない）。子 C: Canvas 拡張所有の新規ルート `GET /api/trace-detail/:traceId`（token 保護、bounded DTO のみ）+ ヘルパーページの「選択したトレースの要約」カード。子 D（raw preview boundary）は D020/D023/D032 と同じ server-rendered・escaped inert text パターンで設計確定したが実装は別途 go-ahead 待ち。子 E（session-to-trace correlation）は D037 時点では OTel 側に対応する安定識別子が存在せず見送ったが、D051 は明示 Session event input と exact-link evidence を使う別 subsystem に限ってこれを更新した。heuristic correlation は引き続き禁止。検証: build 0/0、full `dotnet test CopilotAgentObservability.slnx` 603 passing（301 ConfigCli + 302 LocalMonitor）、`node --test canvas-helpers.test.mjs` 12/12。**実 Canvas runtime（`extensions_manage`/`open_canvas`/`invoke_canvas_action`）による live UI 表示検証のみ人手ゲートとして残**（Sprint11 M6 と同様。GitHub Copilot 側へ引き継ぎ）。Canvas action response は bounded DTO のままで、D051 の Session interfaces 以外の UI 再実装 / telemetry input / schema / raw endpoint 追加 / Copilot actions への raw / PII 送出はしない。 |
| Monitor UX Redesign（prompt 識別） | **完了** | Local Ingestion Monitor を実装案 `tmp/local-monitor/localmonitor.html` ベースで刷新した。決定 D032（ダッシュボード `/` と trace 一覧 `/traces` を raw-bearing 面に拡張し、各トレースを raw OTLP から server 側抽出した代表ユーザープロンプトで識別。same-origin / `Cache-Control: no-store` / `--sanitized-only` 除去を強制、`/api/monitor/*`・SSE は sanitized 不変）/ D033（Cytoscape.js + dagre vendored 依存を撤去し、Flow Chart / Span Tree を素の DOM 化）を記録。`MonitorPromptExtractor` と `MonitorViewFormat` を追加し、Index / Traces / TraceDetail を raw-bearing 化（同一オリジン + no-store + sanitized 時フォールバック）。ナビを 3 項目（ダッシュボード / トレース / 診断）に集約、`/ingestions` を廃止しダッシュボードへ統合、UI を全面日本語化、trace カード化、TraceDetail にパンくず・プロンプト見出し・メトリクスカード・ツリー↔フロー切替を追加。`monitor.css` を新 OKLCH デザインへ刷新（vendored Noto 継続・CDN ゼロ）。spec/decisions/security-data-boundaries/requirements/spec/layer specs と user guide を更新。検証: build 0/0、full `dotnet test CopilotAgentObservability.slnx` 573 passing（301 ConfigCli + 272 LocalMonitor、Playwright 2 件含む）。**実 VS Code Copilot Chat による live プロンプト表示の人手検証のみ残**（既存の Sprint8 M6 ライブ検証 blocker と同じく人手ゲート）。 |
| LocalMonitor Release ZIP Startup | **実装中** | [docs/sprints/sprint14-localmonitor-release-zip-startup/](sprints/sprint14-localmonitor-release-zip-startup/) で、LocalMonitor を Windows x64 self-contained folder publish の Release ZIP として配布し、利用者端末では .NET SDK / Runtime 事前導入や source build を要求しない形にする。install、start、status、stop、uninstall、Task Scheduler startup 登録、enable / disable を分離し、Task は利用者が明示選択した場合のみ current user / least privilege / AtLogOn とする。Release ZIP / workflow artifact / logs に raw / PII / credentials / runtime DB / logs / state を含めない。 |
| Local Monitor Copilot Raw Analysis | **完了 / BYOK live validation 済み** | [docs/sprints/sprint13-local-monitor-copilot-raw-analysis/](sprints/sprint13-local-monitor-copilot-raw-analysis/) で、Local Monitor TraceDetail から raw analysis run を開始し、.NET GitHub Copilot SDK analysis service へ dispatch する local surface を追加した。raw analysis result は local runtime data、GitHub 向けは repository-safe summary のみ。`--sanitized-only` では raw analysis UI / routes を無効化する。自動テストは追加済み。2026-06-30 の live validation で loopback 起動、synthetic trace ingestion、TraceDetail、analysis run 作成、repository-safe summary の raw marker / PII 非混入を確認した。初回 SDK 実行は OS error 5 で失敗したが、BYOK provider と明示的な SDK `BaseDirectory` により analysis run が `succeeded` になった。通常の GitHub Copilot quota-backed provider path はユーザー指示により Sprint13 closeout gate から外した。Evidence: [sprints/sprint13-local-monitor-copilot-raw-analysis/milestones/M6-validation-and-closeout/live-validation.md](sprints/sprint13-local-monitor-copilot-raw-analysis/milestones/M6-validation-and-closeout/live-validation.md)。 |
| Monitor Design Shape | **Sprint12 完了** | [docs/sprints/sprint12-monitor-design-shape/](sprints/sprint12-monitor-design-shape/) で、Sprint10/Sprint11 後の Local Monitor visual foundation を Shape / documentation alignment として整理し、同じ Sprint12 内で `monitor.css` と Razor pages へ反映した。`monitor.css` は OKLCH `--monitor-*` token architecture、vendored Noto typography、8px spacing、VS Code Dark+ / Grafana-inspired density に揃え、共通レイアウト、Traces、TraceDetail、JS 生成 Cache table の component class を整理した。検証は Codex 実行の build 0/0、targeted Monitor UI tests 16 passing、TraceDetail Playwright tests 2 passing、およびユーザー側実行の Playwright Chromium bootstrap と full `dotnet test CopilotAgentObservability.slnx` 成功。既存の telemetry 入力、SQLite schema、API field、raw / sanitized 境界、Canvas adapter contract は変更していない。 |
| LocalMonitor Windows Startup Surface | **完了** | Windows Task Scheduler の user-level logon task として LocalMonitor を起動する `scripts/local-monitor/` 運用面を追加した。Task は current user / least privilege / loopback-only / multiple instances ignore-new を既定とし、DB / logs / state は `%LOCALAPPDATA%\CopilotAgentObservability\LocalMonitor\` 配下に置く。client routing は既存 `profile-vscode-env --profile raw-local-receiver --target monitor` を使い、Task 登録 script は VS Code 設定を書き換えない。 |
| GitHub Copilot Canvas Adapter PoC | **完了** | [docs/sprints/sprint11-github-copilot-canvas-adapter-poc/](sprints/sprint11-github-copilot-canvas-adapter-poc/) で、Local Ingestion Monitor を GitHub Copilot app Canvas から参照する thin adapter を追加した。`/create-canvas` skill workflow、project-scoped scaffold-first 方針、raw default の Local Monitor と併用できる Canvas adapter 方針、bounded action DTO、loopback/token 保護、action/log/committed output への raw/PII 非送出境界を canonical specs に反映した。Canvas id `otel-monitor`、`open()`（拡張所有 loopback ヘルパーページ、`/health/ready` 健全性チェック、trace ドロップダウン、focus セレクタ、「Analyze selected trace with Copilot」ボタン）、`onClose()` を実装した。`monitor_health`、`list_recent_traces`、`get_trace_summary`、`get_trace_span_tree`、`get_cache_summary` は `.github/extensions/otel-monitor-canvas/extension.mjs` で既存 `/api/monitor/*` と `/health/ready` から bounded DTO を返す。M6（検証・ドキュメント）で build 0/0・test 555 passing（300 ConfigCli + 255 LocalMonitor）を確認し、Canvas runtime tools が利用可能な環境で live Canvas validation を実施した。`extensions_reload`/`extensions_manage`/`list_canvas_capabilities`/`open_canvas`/`invoke_canvas_action` で全 5 action の起動・呼び出しを検証、schema 拒否・bounded DTO・loopback/token 保護・action/log/committed output への raw/PII 非送出のネガティブチェック通過。user guide に Canvas adapter セクションを追加。M6 evidence: [sprints/sprint11-github-copilot-canvas-adapter-poc/milestones/M6-validation-and-docs/live-validation.md](sprints/sprint11-github-copilot-canvas-adapter-poc/milestones/M6-validation-and-docs/live-validation.md)。 |
| Monitor Agent Execution View | **完了** | [docs/sprints/sprint9-monitor-agent-execution-view/](sprints/sprint9-monitor-agent-execution-view/) で Local Ingestion Monitor を「取り込み健全性」から「エージェント実行詳細」へ拡張する。受信済み OTel から sanitized な per-span projection（どのツール/MCP・成否・sub-agent のモデル/トークン・turn 単位トークン）を生成し、VS Code Agent Debug View を参考にした機能的 UI で表示する。決定 D021（非目的の絞り込み）/ D022（span projection）/ D023（raw 既定表示＋`--sanitized-only` 安全弁、D020 更新）を記録済み。2026-06-29 に実 VS Code Copilot Chat ライブ検証を以て検証完了。後続のデザイン改善は Sprint10（Monitor Design Views）へ。 |
| Monitor Design Views | **完了** | [docs/sprints/sprint10-monitor-design-views/](sprints/sprint10-monitor-design-views/) で、Sprint9 で deferred とした design 項目（graphical Flow Chart / Cache Explorer / 視覚 polish / timeline filter-sort）を、既存 spans API 上の **sanitized・client-side presentation** として実装する。新たな telemetry 入力・schema・API field・raw 境界変更は加えない。決定 D024（design 非目的の絞り込み）/ D025（Cytoscape.js + dagre を vendored）/ D026（Cache Explorer は sanitized-metrics-only、prefix-diff・cross-trace は out）/ D027（VS Code 風 dark theme、DADS 非適用）/ D028（Noto fonts vendored）を M1 で記録。M2 で VS Code 風テーマ・Noto vendor・TraceDetail tab shell を実装し、M3 で Cytoscape.js + dagre vendor と Flow Chart tab（sanitized spans API だけを読む DAG、node click → Timeline highlight）を追加した。M4 で Timeline tab を sanitized spans API からの client-side flat list に移し、errors-only filter と time/tokens sort を追加した。M5 で Cache tab に trace-local cache-hit rate / cache creation / token breakdown を追加した。M6 で Playwright browser smoke test と既存 security/UI/SSE validation を追加した。S10-1 で `--sanitized-only` 時も TraceDetail の sanitized tabs は表示し、raw section / full raw link だけを除外するよう修正済み。2026-06-29 に実 VS Code Copilot Chat による実機検証（Live Validation）を実施し、全タブの正常表示と sanitized 境界を実証して完了。 |

## Historical Work

| Area | 状態 | 概要 |
| --- | --- | --- |
| Langfuse baseline | 完了 | [docs/sprints/sprint1-langfuse-poc/](sprints/sprint1-langfuse-poc/) に Langfuse 直接送信、設定 CLI、計測 schema、human approval workflow までの履歴を保存した |
| Raw Data Loop | 完了 | [docs/sprints/sprint2-raw-data-loop/](sprints/sprint2-raw-data-loop/) で raw OTLP file ingest、SQLite raw store、normalized dataset、Langfuse 非依存 loop を実装した |
| ConfigCli Maintainability | 完了 | [docs/sprints/sprint2-5-maintainability/](sprints/sprint2-5-maintainability/) で ConfigCli 分割、CSV / JSON 共通処理集約、redacted real-trace 互換性確認を行った |
| Trace Diagnosis | 完了 | [docs/sprints/sprint3-trace-diagnosis/](sprints/sprint3-trace-diagnosis/) で deterministic 診断候補生成、改善候補生成、自動採用判断 record を実装した |
| Observability Dashboard | 完了 | [docs/sprints/sprint4-observability-dashboard/](sprints/sprint4-observability-dashboard/) で dashboard view、metric、dimension、drilldown、dataset contract を定義した |
| Static Dashboard | 完了 | [docs/sprints/sprint5-static-dashboard/](sprints/sprint5-static-dashboard/) で static HTML dashboard と dashboard input staging contract を実装した。GitHub Actions Pages publish workflow は D049 で削除済み。 |
| Collection Profiles | レビュー完了 | [docs/sprints/sprint6-collection-profiles/](sprints/sprint6-collection-profiles/) で `CAO_COLLECTION_PROFILE`、raw-only minimum、Docker Desktop / WSL2 / remote managed routing profiles の code / docs を実装し M6 review で accept した。`raw-local-receiver` は Sprint7 へ handoff 済み。remote managed live validation のみ外部 access-control / consent 判断待ち |
| Local Raw Receiver | synthetic validation 完了 / live validation 待ち | [docs/sprints/sprint7-local-raw-receiver/](sprints/sprint7-local-raw-receiver/) で repository-local foreground receiver、`raw-local-receiver` profile output、raw store integration、synthetic JSON/protobuf smoke validation を実装した。VS Code direct telemetry live validation は未確認 |
| Local Raw Receiver Monitor | **完了** | [docs/sprints/sprint8-local-raw-receiver-monitor/](sprints/sprint8-local-raw-receiver-monitor/) で ConfigCli から共有 `Telemetry` / `Persistence.Sqlite` project を抽出した（M1）。M2 で `CopilotAgentObservability.LocalMonitor` ASP.NET Core host、loopback bind、Host header 検証、`POST /v1/traces` JSON/protobuf 受信、SQLite raw store 永続化、`413` body-size 境界、`--enable-raw-view` option、`profile-vscode-env --target monitor` / `--endpoint` を追加した。M3 で bounded-channel ingestion queue + 単一 SQLite writer worker（commit 後のみ `2xx`、queue full `503` / commit timeout `504` / shutdown `503` / DB busy `503` / 非 busy 失敗 `500`）、`schema_version` additive migration（空の projection tables を allowlist 列で作成、raw/PII なし、`raw_records` 維持、WAL + busy_timeout 並行読取）、readiness 閾値オプション、最初の `/health/*` を追加した。M4 で sanitized projection builder（`Telemetry/Monitoring/`、allowlist のみ、非空 `trace_id` ごとに `monitor_traces` へ fan-out、trace_id 無しは ingestion 行のみで stall させない）、冪等 projection 投入（`INSERT OR IGNORE` + ガード付き集約 upsert）、`ProjectionWorker`（startup catch-up / `SQLITE_BUSY` retry / 非busy失敗の隔離 / migration 完了待ち）、`/api/monitor/ingestions`・`/api/monitor/traces` cursor API（sanitized、`after`/`limit`、`limit+1` 終端、`raw_record_id` と projection-id の cursor domain、payload 非ロード、不正 query `400`）、projection-lag readiness の有効化（`ready`/`degraded`/`not_ready`。loopback/db/writer/projection-worker/projection-status を必須ゲート化）、opt-in raw-detail route `GET /traces/{rawRecordId}/raw`（`--enable-raw-view` 時のみ存在＝無効時 `404`、cross-site `403`、`Cache-Control: no-store`、HTML エスケープ inert 描画）を追加した。検証は build 0 警告・0 エラー、test 421 passing（300 ConfigCli + 121 LocalMonitor、M3 baseline 371 超）。Codex implementation review の指摘2件（readiness の必須ゲート漏れ / projection-status の stale zero-lag）を修正済み。M5 で sanitized Razor Web UI（`/`・`/ingestions`・`/traces`・`/diagnostics`、allowlist 列のみ・`Html.Raw` 不使用・`raw` リンクは `--enable-raw-view` 時のみ M4 opt-in route へ）、notification-only SSE（`GET /events`、`text/event-stream`、projection 後に `data: {}` のみ・raw/PII 非送出・同期購読登録で取りこぼし無し・gap recovery は `/api/monitor/*` cursor）、`wwwroot/monitor.js`（通知ごとに cursor API 再読込、DOM に raw 非挿入）を追加。static asset は Production 既定環境でも配信されるよう static web assets を明示ロード。検証は build 0/0、test 433 passing（300 ConfigCli + 133 LocalMonitor、M4 baseline 421 超）＋実 `dotnet run` スモークで全ページ・静的アセット 200。M5 までで raw 表示の de-scope（framework 既定エスケープ）を維持。M6 は検証マイルストーンとして DR6 negative security matrix（default UI/API/SSE が raw/PII 非返却・raw route フラグ無し 404／フラグ有り cross-site 403・SSE GET 限定で cross-origin POST 拒否・非 loopback Host 400・error 応答に raw/DB path/user 名なし）、HTTP レベル readiness 失敗（瞬間 backpressure/commit timeout と閾値未満 lag は 200 degraded、持続 stall・projection_lag_exceeded・projection_status_unknown は 503）、同一 DB restart recovery を追加（いずれも既存実装で合格、production 変更なし）。build 0/0・test 445 passing（300 ConfigCli + 145 LocalMonitor）。`profile-vscode-env --target monitor` は 4320+http/protobuf を出力、実プロセス合成 OTLP で取り込み→projection→`/health/ready` 200 を確認。ただし**実 VS Code Copilot Chat のライブ検証は人手必須でブロック**（`milestones/M6-security-live-validation/live-validation.md`）。Sprint8 はこのライブ検証が hard gate のため**未完了**。 |

## Open Follow-ups

- **Issue #89 Raw retention cleanup (complete / close-ready):** catalog v1,
  immutable raw-read inventory, frozen Session v1 compatibility projection,
  migration/read gates, worker, five exact adapters, and sanitized diagnostics
  are complete. The implementation candidate is `5c5540878a6731804084644d8a136be9ad748cf9`;
  repository-safe closeout evidence and full validation are pinned at
  `2549b1d94f2df0d06d092882aef636d2aec2ba3a` in
  [issue-89-final-retention-inventory.md](sprints/issue-89-final-retention-inventory.md).
  The independent F2 spec/code review passed with no blockers, the pre-close
  audit found no implementation blockers, and the final whole-Issue review at
  `74f9f6ee8076fe7282bab9565ebbbd4775e61dd0` passed spec and code review with
  no blockers. Issue #90 is complete locally at
  `4d966472bdbe1ccc27f57c05f3afc268025ed37a`; its closeout evidence is in
  [the Issue #90 review](sprints/issue-90-retention-mutation/milestones/M1-integration-security-closeout/review.md).

- **Issue #91 security consumer:** consume the exact-SHA
  `retention-security-matrix/v1` rows from the Issue #90 closeout. Issue #90
  does not pre-complete Issue #91 implementation or external closure.

- **Issue #88 restore/backup consumer:** preserve the five
  `retention-restore-handoff/v1` tombstone, no-resurrection, retention-clock,
  consistent-restore, and backup-non-purge invariants from the Issue #90
  closeout. Issue #90 does not inventory or purge backups and does not claim a
  backup exists.

- **未対応の機能仕様・検証（未確認の検証フェーズ）**
  - Sprint18 スパンインスペクタの `SpanDetailExtractor` は実 VS Code Copilot Chat ペイロードのキー名が live 検証まで未確定のため defensive 抽出とし、整形抽出が空でも raw タブで代替できる。実ペイロードでの整形表示のライブ検証は人手ゲート（D043、既存 prompt extractor と同種の caveat）。
  - Sprint18 のプロンプト検索は server 側 TraceId 部分一致 + client 側の読み込み済み行 prompt label フィルタに限定（D042 C8）。全コーパスの prompt 全文検索（raw store 横断）は未対応の follow-up。
  - リモート・プロファイル（`remote-managed-*`）の実環境での疎通検証（Live Validation）。
  - ローカル・レシーバーの常駐起動・配置オプション（IIS / IIS Express ホスティング、Windows サービス化、タスクトレイアプリ化など）の評価とパッケージング検討。
- **未検討・未決定の意思決定事項（Open Product / Security Decisions）**
  - 静的ダッシュボード artifact の共有運用におけるアクセス制御の具体設計。
  - 共有ダッシュボードのデータ保持期間（Retention）、削除方法、利用者への周知方法。
  - 実データ運用時のマスキング・秘匿化（Redaction）方針の決定。
  - Collector / remote / shared operation での TLS、SSO、sampling、credential handling の採用方針。
  - リモート・プロファイル利用時のユーザー同意ワークフロー（User Consent Workflow）の実装・定義。
  - ID情報のハンドリング（`user.id` / `user.email` から表示名（Display Name）へのマッピング設計）。
  - 外部成果物との紐付け（External Outcome Linkage - GitHub Issues, PR, Notion等）の採用可否とプロダクト/セキュリティポリシーの策定。
- **仕様上のプレースホルダー・保留事項**
  - `dashboard-dataset` における Outcome Linkage Candidate のプレースホルダー実装の具体化。
  - 互換性のために残されている古いエントリポイント（`langfuse-*`, `collector-*`）のクリーンアップ時期と方法の決定。
- **技術負債（2026-06-30 SSOT 監査より、欠陥ではなく保守性項目として延期）**
  - `CliApplication.cs` のコマンドディスパッチに重複する IO / エラーハンドリングが蓄積している（F6）。新規コマンド追加時に既存パターンのコピーを防ぐため、共通の file-not-found / validation / exception マッピングを抽出する。現状動作は正しい。
  - `RawTelemetryStore.cs` が raw schema / monitor migration / raw CRUD / ingestion / trace / span projection / rollup / paging / SQLite 接続ポリシーを集中所有している（F7）。additive migration や read-model 変更の回帰リスクを下げるため、安定契約の背後で projection store 単位に分割する。
  - ~~Canvas extension contract test が文字列部分一致検査中心で、構文エラーや helper-server token / request shaping 回帰を検出できない（F8）。~~ Sprint15 M1 A0 で対応済み：`canvas-helpers.mjs`（純関数）+ `canvas-helpers.test.mjs`（`node --test`）+ `CanvasExtensionContractTests.cs` の node 実行テストを追加し、`extension.mjs` 大改修前の実行可能 smoke coverage を `dotnet test` ゲートへ統合した。

## Rule For New Work

新しい product behavior を追加する場合は、実装前に以下を更新する。

1. [docs/requirements.md](requirements.md)。
2. [docs/spec.md](spec.md)。
3. 該当する [docs/specifications/](specifications/) file。
4. 必要な user guide または contributor guide。

Sprint-local notes は履歴として残してよいが、仕様を sprint-local document だけに閉じ込めない。
