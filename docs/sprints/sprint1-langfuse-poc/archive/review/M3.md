# Review Note: M3

## レビュー範囲

- Config CLI の Phase 0 設定サンプル生成
- `OTEL_RESOURCE_ATTRIBUTES` 検証
- Config CLI の単体テスト
- M3 タスク更新

## 変更ファイル

- `src/CopilotAgentObservability.ConfigCli/Program.cs`
- `src/CopilotAgentObservability.ConfigCli/Properties/AssemblyInfo.cs`
- `tests/CopilotAgentObservability.ConfigCli.Tests/ConfigSamplesTests.cs`
- `tests/CopilotAgentObservability.ConfigCli.Tests/ResourceAttributeValidatorTests.cs`
- `tests/CopilotAgentObservability.ConfigCli.Tests/CliApplicationTests.cs`
- `docs/task.md`
- `docs/review/M3.md`

## 使用したサブエージェント

- なし

## サブエージェントの起動成否

- 未起動。現セッションの上位指示では、ユーザーが明示的にサブエージェント利用を依頼した場合のみ起動可能なため、自己レビューで代替した。

## raw findings summary

- 自己レビュー: Config CLI は標準出力・標準エラーのみを使用し、ユーザー環境の VS Code settings や shell profile を自動編集しない。
- 自己レビュー: `vscode-settings` と `copilot-cli-env` は `docs/spec.md` の Phase 0 既定値を出力する。
- 自己レビュー: `validate-resource-attributes` は必須キー欠落と不正な `key=value` 形式を error、推奨値外の `client.kind` と `experiment.id` を warning として扱う。
- 自己レビュー: 新しい NuGet 依存は追加していない。

## メインエージェントの妥当性判断

- 指摘事項はなし。
- 実装は `docs/spec.md` の M3 範囲内であり、Langfuse、Collector、認証ヘッダー、永続設定変更は扱っていない。

## 対応方針

- 追加修正なし。

## 適用した修正

- なし。

## 残リスク

- `OTEL_RESOURCE_ATTRIBUTES` の厳密な OTel 仕様全体のパースは実装していない。M3 の要件であるカンマ区切り `key=value` の検証に限定している。
- Copilot 実行に依存するライブ確認は M4 の範囲として未実施。

## 保留事項

- M4 でソリューション全体の検証、Aspire Dashboard 起動確認、Copilot Chat からの手動ライブ確認を実施する。
