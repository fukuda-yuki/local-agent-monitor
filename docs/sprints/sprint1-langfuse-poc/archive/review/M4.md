# Review Note: M4

## レビュー範囲

- `M4: 検証とレビュー`
- .NET 10 ソリューションの build / test 結果
- Aspire Dashboard のローカル起動確認
- Copilot Chat 依存の手動ライブ確認項目の未確認理由と必要証跡
- M4 の task / knowledge 更新

## 変更ファイル

- `docs/task.md`
- `docs/sprints/sprint1-langfuse-poc/knowledge.md`
- `docs/review/M4.md`

## 使用したサブエージェント

- なし

## サブエージェントの起動成否

- 未起動。現セッションの上位指示では、ユーザーが明示的にサブエージェント利用を依頼した場合のみ起動可能なため、自己レビューで代替した。

## raw findings summary

- 自己レビュー: `dotnet build CopilotAgentObservability.slnx` は成功し、警告 0、エラー 0 だった。
- 自己レビュー: `dotnet test CopilotAgentObservability.slnx --no-build` は成功し、18 件合格、失敗 0、スキップ 0 だった。
- 自己レビュー: `dotnet run --project src\CopilotAgentObservability.AppHost\CopilotAgentObservability.AppHost.csproj --launch-profile https` で AppHost を起動し、`https://localhost:17100` が HTTP 200 を返すことを確認した。
- 自己レビュー: VS Code GitHub Copilot Chat からの実 trace 取り込み、span tree、token usage、duration、error、content capture、Resource Attributes は、このセッションから VS Code Copilot Chat を操作して実送信できないため未確認。
- 自己レビュー: M4 では仕様・外部公開インターフェース・依存関係・実装コードを変更していない。

## メインエージェントの妥当性判断

- 自動検証と AppHost 起動確認は `docs/spec.md` の M4 範囲に合致している。
- Copilot Chat 依存の項目は、`docs/spec.md` で手動ライブ確認として定義されており、エージェント単独の自動検証で完了扱いにしてはならない。
- 未確認項目は `docs/task.md` で未完了のまま残し、必要証跡と未確認理由を `docs/sprints/sprint1-langfuse-poc/knowledge.md` と本 review note に記録するのが妥当。

## 対応方針

- 自動検証済み項目と AppHost 起動確認のみ `docs/task.md` で完了にする。
- 手動ライブ確認項目は未完了のまま残す。
- ユーザーが実施すべきライブ確認の証跡項目を明記する。

## 適用した修正

- `docs/task.md` の M4 自動検証、AppHost 起動確認、未確認理由記録、レビュー記録を完了に更新した。
- `docs/sprints/sprint1-langfuse-poc/knowledge.md` に M4 検証結果と手動ライブ確認の必要証跡を追記した。
- `docs/review/M4.md` を追加した。

## 検証結果

- `dotnet build CopilotAgentObservability.slnx`
  - 成功
  - 警告 0
  - エラー 0
- `dotnet test CopilotAgentObservability.slnx --no-build`
  - 成功
  - 合格 18
  - 失敗 0
  - スキップ 0
- AppHost 起動確認
  - コマンド: `dotnet run --project src\CopilotAgentObservability.AppHost\CopilotAgentObservability.AppHost.csproj --launch-profile https`
  - 確認 URL: `https://localhost:17100`
  - 結果: HTTP 200
  - 確認後、起動した AppHost プロセスは停止した。

## 手動ライブ確認に必要な証跡

- 確認日時
- 実行環境
  - VS Code version
  - GitHub Copilot Chat extension version
  - 使用した workspace / repository
- VS Code settings
  - `github.copilot.chat.otel.enabled`
  - `github.copilot.chat.otel.exporterType`
  - `github.copilot.chat.otel.otlpEndpoint`
  - `github.copilot.chat.otel.captureContent`
- Aspire Dashboard URL
  - frontend: `https://localhost:17100`
  - OTLP/HTTP endpoint: `https://localhost:21025`
- 実行した Copilot Chat の依頼内容
- Aspire Dashboard 上の trace id または識別できる trace 情報
- 確認できた項目
  - trace 取り込み
  - span tree
  - agent invocation
  - LLM call
  - tool call
  - token usage
  - duration
  - error
  - prompt / response / tool arguments / tool results
  - `client.kind=vscode-copilot-chat`
  - `experiment.id=baseline`
- 未確認項目と理由

## 残リスク

- VS Code GitHub Copilot Chat から Aspire Dashboard に実 trace が届くことは未確認。
- content capture の実 attributes 名や表示位置は、Copilot Chat extension の実装やバージョンに依存して変わる可能性がある。
- `https://localhost:21025` への送信は、ローカル開発証明書の信頼状態や VS Code 側の TLS 扱いに依存する可能性がある。

## 保留事項

- ユーザー環境で VS Code GitHub Copilot Chat を実行し、手動ライブ確認の証跡を追記する。
- 手動ライブ確認が完了したら、`docs/task.md` の未完了項目を更新する。

## 2026-04-26 追記: HTTPS endpoint 切り分け

- 追加確認範囲
  - VS Code GitHub Copilot Chat から Aspire Dashboard に表示されない原因の切り分け
  - `https` profile と `http` profile の endpoint 差分
  - Aspire Dashboard の Console Logs / Structured Logs / Traces の確認先
- raw findings summary
  - VS Code / VS Code Insiders の GitHub Copilot Chat log では `endpoint=https://localhost:21025` が有効になっていた。
  - PowerShell から `https://localhost:21025/v1/logs` と `/v1/traces` に送った人工 OTLP telemetry は Dashboard に表示された。
  - Node/Electron 相当の `fetch` では `https://localhost:21025/v1/logs` が `DEPTH_ZERO_SELF_SIGNED_CERT` で失敗した。
  - ユーザー環境で `http` launch profile と `http://localhost:19164` を使うと Dashboard に表示されることを確認した。
- 妥当性判断
  - Dashboard の OTLP 受信は動作しており、主因は Copilot Chat extension 側の Node/Electron HTTP client が `https` profile のローカル開発証明書を信頼できないことと判断する。
  - Phase 0 の主手順は `http` launch profile に変更するのが妥当。
  - OTLP Logs の確認先は `consolelogs` ではなく `structuredlogs` である。
- 適用した修正
  - `docs/spec.md` に `http` profile を Phase 0 主手順として記載した。
  - `docs/sprints/sprint1-langfuse-poc/knowledge.md` に HTTPS 切り分け結果を追記した。
  - `docs/task.md` に `http` profile での確認と Config CLI 既定値見直しの follow-up を追加した。
- 残リスク
  - `http` profile で Dashboard に表示されることは確認済みだが、span tree、token usage、duration、error、content capture、Resource Attributes の詳細確認は未完了。
  - Config CLI の既定 endpoint は別途実装判断が必要。
