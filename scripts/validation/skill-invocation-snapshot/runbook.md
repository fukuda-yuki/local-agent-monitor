# Issue 158 live-validation runbook

These lanes are operator-only and fail closed. Do not run them until the reviewed harness is committed as an immutable detached candidate.

## Common safety gate

`preflight.ps1` is Windows-only and performs no creation or cleanup. Before any later wrapper may act, the operator must provide two separate owner-marked directories under the system temporary root. Each contains only the closed JSON `.cao-issue-158-owner.json`, cross-binding the process-local run id, candidate, kind, and SHA-256 hashes of both physically canonical runtime/result paths. Every existing component and reparse target is resolved before containment, alias, and repository-overlap checks. The normalized lexical and resolved physical root paths must be identical, and the owner marker must be a regular file at its exact physical leaf with no link identity, including hard links, symbolic links, junctions, or reparse points.

`scan-leaks.ps1` accepts only a passed, closed-schema Windows or Linux JSON result. Before acceptance it re-proves the same detached clean candidate, canonically resolves the absent runtime leaf through its existing physical parent, verifies that the surviving result owner cross-binds that exact runtime identity, and permits only the owner plus declared JSON. Its bounded decoder applies the same prohibited-content rules to raw, URL, base64, hex, UTF-8/16/32 text. Neither script prints paths, values, exceptions, marker text, stderr, or file content.

Run `self-test.ps1` to exercise the common gate in a disposable local Git repository. Its helper load, setup, assertions, and final guarded cleanup share one closed boundary; success is emitted only after confirmed fixture absence, while every failure emits only `self_test_result=BLOCKED check=internal`. The self-test owns and removes its fixture and performs no network, SDK, WSL, service, global-setting, or repository mutation.

The Windows wrapper requires `-OperatorAuthorized` and the exact lowercase 40-hex detached clean `HEAD`. Only after its initial authorization, SHA, and `COPILOT_CLI_PATH` process-entry gates does it create unique runtime/result children directly below the physical system temp root and cross-bind their strict owners. It binds its generated run id and runtime marker to its own process before preflight, preserves those exact values through leak scanning and the gated child, then restores any inherited process values without printing them. Its exact `dotnet test` argument list permits the fresh detached checkout to restore/build and supplies the build-only property `-p:CopilotCliVersion=1.0.75` exactly once; it supplies no binary path or runtime CLI override. Its one VSTest filter is exactly `FullyQualifiedName=CopilotAgentObservability.LocalMonitor.Tests.Issue158OwnedSessionLiveHarnessTests.WindowsOwnedSession_TraversesTheProductionHost&Issue158Lane=WindowsOwnedSession`. It drains both child pipes concurrently and incrementally without imposing a wrapper lifecycle timeout: stdout is discarded while a saturating overflow flag records only whether it exceeds 1,048,576 characters, and stderr retains only a nonempty flag. Owned-root deletion is authorized only after the normal drain returns, the same root process reports exit, its exit code is captured, and process disposal succeeds. Any process/drain lifecycle exception after start triggers only best-effort entire-tree termination and an abnormal root wait bounded to 30 seconds; it always retains both roots and emits `internal`, because root exit cannot prove descendant exit. Ordinary wrapper stage codes require the fully normal lifecycle followed by successful owned cleanup. It runs only that exact FQN-and-trait intersection, guarded-cleans runtime before scanning, and guarded-cleans the result before emitting the validated JSON. Safe failures emit only `windows_result=BLOCKED check=<code>`.

The Windows live Fact is skipped during ordinary discovery. When explicitly authorized later, its first body operation revalidates every test-only input before constructing the real host/client chain. The test creates one synthetic retained root and skill, triggers analysis through the host HTTP route, and, after persisted success, awaits the synchronous `CandidatePublished` checkpoint for at most 30 seconds before observing evidence or current-generation routes. Evidence certifies exactly one retained custom Skill, while probe inventory may include disabled ambient Skills and execution inventory may omit them: probe inventory is at least retained inventory, and execution inventory is between retained and probe inventory inclusive. A successful run requires a total of 1 through 64 prepared invocations: exactly one is `user-invoked` and zero through 63 are `agent-invoked`. The committed v2 identity count, v2 imports, snapshot rows, and SDK claims must all equal that total; every committed identity shares the exact session and has a distinct snapshot id. The test holds the committed v2 identities only in memory for the metadata/historical/current-file route matrix, then reads the committed current authorities synchronously. For that exact session, lifecycle v1 remains exactly one `session.start` plus one `session.task_complete`, and `v1_imported` is exactly their sum of two. Do not invoke this lane until the candidate is committed, detached, clean, and separately approved for signed-in execution.

On a non-success analysis terminal or a failure after persisted success, the gated test may create one temporary strict nine-string `issue-158-windows-blocker.v6` document beside the result owner. Non-success terminals preserve the existing checkpoint, driver-phase, poison, and post-freeze synthesis. The `publication_barrier` stage truthfully permits the observed `CandidateReady` or `CandidatePublished` checkpoint; every later post-success stage requires `CandidatePublished`. An execution-evidence failure carries one closed field-level discriminator and synthesizes only `succeeded_post_success_execution_evidence_<detail>`; no values or counts leave the private artifact. Every other post-success failure keeps the existing `succeeded_post_success_<stage>` form, and a later shutdown failure cannot replace the first stage. The wrapper rejects v5 and older, validates the blocker privately, guarded-cleans and proves both roots absent, restores process identity, and only then emits the synthesized code. A nonzero child bypasses output classification; with only its valid result owner it is `child_nonzero_unreported`. After a zero child exit and normal disposal, the wrapper has drained and discarded stdout while retaining only ordered `StdoutOverflow` and `StderrNonempty` booleans; it never retains or emits content or counts. Output failures use this fixed precedence: `wrapper_test_output_contract`, `wrapper_test_output_stderr_nonempty`, then `wrapper_test_output_overflow`. Console summaries have no evidentiary authority. The strict result, owner, candidate, and topology validation chain is the sole positive authority; zero selected tests, a filter mismatch, or a skipped Fact produces no result and closes at `wrapper_scan` after safe cleanup. Later safe wrapper failures are `wrapper_runtime_cleanup`, `wrapper_scan`, `wrapper_result_validation`, `wrapper_result_cleanup`, `wrapper_final_validation`, or `wrapper_identity_restore`. Malformed, linked, oversized, unexpected, or cleanup-incomplete state remains `internal`; the blocker is never retained.

The Linux wrapper requires explicit operator authorization and an exact lowercase detached-clean candidate. It resolves both the Git common directory and tracked Bash helper component by component, requires each lexical path to equal its physical non-reparse identity with the correct directory/file type, proves the candidate commit exists in that object database, and uses the common directory rather than the linked worktree as the read-only clone source. Each path is converted independently through its own validated Windows drive, so a cross-drive linked worktree remains valid. `/mnt/<drive>` is source/helper transport only; checkout and work roots remain outside every Windows mount and must be native `ext4`.

Before creating its owned lane top, the helper preflights its required tools and the separately provisioned disposable SDK at exactly `/tmp/cao-issue158-dotnet-sdk-10.0.203`. That root and its executable must be real non-symlink paths, and the executable must report exactly `10.0.203`; the harness does not install or download it. A missing or invalid SDK is the closed `prerequisite_dotnet` stage. Plain noninteractive GNU `timeout` owns the helper process group for 12 minutes, while another process-group supervisor bounds the test subprocess at 10 minutes; both use a 30-second forced-termination grace. The Windows wrapper normally drains the child pipes without a wrapper timeout. On a process/drain lifecycle fault it attempts entire-tree termination and waits up to 30 seconds for the abnormal root-process fence before disposal; uncertainty remains `internal` with owned roots retained.

After ownership, every recognized failure emits only one closed sanitized stage after guarded cleanup and proven absence. Cleanup re-proves the byte-exact canonical owner with no trailing byte and link count exactly one, absence of mounts and submounts, a complete NUL-safe allowed-name inventory, non-alias entries, successful one-filesystem deletion, and final absence. Owner-write gaps, owner mismatch or hardlinks, mounts, incomplete enumeration, deletion failure, unexpected artifacts, aliases, or inability to prove absence expose no accepted carrier and remain `internal` at the Windows wrapper.

The Linux live Fact is skipped by default and revalidates Linux, kernel, candidate, checkout cleanliness, exact owner/top layout, physical non-alias paths, and independently proves both the detached checkout and work/runtime root are ext4 before constructing filesystem, database, runtime, or HTTP state. It uses the real Linux retained-root opener and current-file reader for strict UTF-8, unsafe/symlink, missing, oversized, binary, and same-fd race cases. It seeds one synthetic v2 snapshot, publishes a discovery-only fake runtime generation that cannot start or create Copilot sessions, and exercises the production metadata, historical-content, and current-file routes. The subprocess writes only a pending result with `cleanup_complete=false`; after guarded ext4 cleanup and proven absence, the Windows wrapper changes that fact in memory, runs the shared strict validator, and emits the final result. Do not invoke this lane until the committed candidate receives separate WSL authorization.

If the live Fact throws, it may create—but never overwrite—the same gate-bound `prepared.json` as one exact ASCII diagnostic naming only a closed test stage. Route failures retain the first route stage even if shutdown also fails; shutdown is reported only when it is the first failure. The helper privately validates that carrier's regular-file identity, single-link authority, exact bytes, and closed stage before cleanup. Only after guarded cleanup and proven absence may it emit `test_<stage>`; missing or safely malformed carriers collapse to `test`, while aliases, links, unsafe types, or cleanup uncertainty remain `internal`. The inner test supervisor's exact timeout and forced-kill exits become `test_timeout` and `test_killed` only after the same cleanup proof. Outer WSL timeout or kill remains `internal`. On exit zero, console text has no evidentiary authority: the exact prepared success result and the existing strict Windows validator are the sole success gate, leaving the public success JSON unchanged.

Only the closed JSON result may leave the private disposable area. Run `scan-leaks.ps1` before accepting it. Any failure, timeout, cleanup error, unexpected artifact, or leak hit blocks evidence acceptance.
